# Core Supabase
SUPABASE_URL="https://your-supabase-domain.example.com"
SUPABASE_ANON_KEY="replace-with-anon-key"
SUPABASE_SERVICE_ROLE_KEY="replace-with-service-role-key"

# AI / QC (Vertex AI)
# Optional overrides; defaults are VERTEX_LOCATION=us-central1 and VERTEX_MODEL=gemini-2.0-flash-001
VERTEX_PROJECT_ID=""
VERTEX_LOCATION=""
VERTEX_MODEL=""

# GCP analyzer integration
GCS_BUCKET="your-video-analysis-bucket"
GCP_SERVICE_ACCOUNT_JSON='{"type":"service_account","project_id":"..."}'
GCP_CALLBACK_SECRET="replace-with-shared-callback-secret"

# Notion integration
NOTION_API_KEY="replace-with-notion-secret"

# Frame.io integration
FRAMEIO_CLIENT_ID="replace-with-frameio-client-id"
FRAMEIO_CLIENT_SECRET="replace-with-frameio-client-secret"

# Optional scheduled sync auth
CRON_SECRET="replace-with-cron-secret"
